<form method="post" action="{{ path('app_delivery_delete', {'id': cake_delivery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cake_delivery.id) }}">
    <button class="btn">Delete</button>
</form>
