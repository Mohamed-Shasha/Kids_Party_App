{% extends 'base.html.twig' %}

{% block title %}CakeDelivery{% endblock %}

{% block body %}
    <h1>CakeDelivery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cake_delivery.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ cake_delivery.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_delivery_index') }}">back to list</a>

    <a href="{{ path('app_delivery_edit', {'id': cake_delivery.id}) }}">edit</a>

    {{ include('cake_delivery/_delete_form.html.twig') }}
{% endblock %}
