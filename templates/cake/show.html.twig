{% extends 'base.html.twig' %}

{% block title %}Cake{% endblock %}

{% block body %}


{#    <table class="table">#}
{#        <tbody>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <td>{{ cake.id }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>ImagePath</th>#}
{#                <td>{{ cake.imagePath }}</td>#}
{#                y            </tr>#}
{#            <tr>#}
{#                <th>Price</th>#}
{#                <td>{{ cake.price }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>Descirption</th>#}
{#                <td>{{ cake.descirption }}</td>#}
{#            </tr>#}
{#        </tbod>#}
{#    </table>#}
    <div class="container">
    <div class="col-md-8">
        <h1 class="mt-4 mt-md-0">{{ cake.name }}</h1>
        <div class="col-md-4">
            <img src="{{ cake.imagePath }}" alt="{{ cake.name }}" class="img-fluid">
        </div>
        <h2>{{ cake.price }} €</h2>
        <hr>
        <b>Description: </b>{{ cake.description }}
        {{ form_start(form, {'attr': {'class': 'mt-4 p-4 bg-light'}}) }}
        <div class="form-group">
            {{ form_label(form.quantity) }}
            {{ form_widget(form.quantity, {
                'attr': {
                    'class': 'form-control ' ~ (form.quantity.vars.valid ? '' : 'is-invalid')
                }
            }) }}
            <div class="invalid-feedback">
                {{ form_errors(form.quantity) }}
            </div>
        </div>
        {{ form_widget(form.add, {'attr': {'class': 'btn btn-danger ','style':'margin:20px'}}) }}
        {{ form_end(form) }}
    </div>
    </div>

{#    <a href="{{ path('app_cake_index') }}">back to list</a>#}

{#    <a href="{{ path('app_cake_edit', {'id': cake.id}) }}">edit</a>#}

{#    {{ include('cake/_delete_form.html.twig') }}#}
{% endblock %}
